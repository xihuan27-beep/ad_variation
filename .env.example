# Vercel 배포 시 프로젝트 설정 → Environment Variables 에 등록한다.
# 로컬 개발 시에는 이 파일을 .env.local 로 복사해서 값을 채운다 (.env.local 은 git에 올리지 않는다).

# AI 문구 제안(app/api/suggest-copy)에 필요하다. 없으면 이 기능만 비활성화되고
# 나머지 앱(엑셀 업로드·매칭·PPT 생성)은 정상 동작한다.
ANTHROPIC_API_KEY=
